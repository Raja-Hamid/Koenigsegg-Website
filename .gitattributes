# Normalize line endings for text; let Git detect what is text.
* text=auto

# Treat binaries as binary so Git never tries to diff or normalize them.
*.jpg   binary
*.jpeg  binary
*.png   binary
*.ico   binary
*.webp  binary
*.avif  binary
*.woff  binary
*.woff2 binary
